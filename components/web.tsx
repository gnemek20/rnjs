import styles from '@/styles/web/web.module.css';

const Web = () => {
  return (
    <div className={`${styles.outline}`}>
      <div className={`${styles.web}`}>
      </div>
    </div>
  );
}

export default Web;